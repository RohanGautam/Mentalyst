stuff:
	# only tested on bionic
	sudo apt-get install python3 python3-all-dev python3-pip build-essential swig git libpulse-dev -y
	sudo apt-get install ffmpeg libavcodec-extra -y
	pip3 install --user pocketsphinx SpeechRecognition pydub

test:
	python3 audio_transcribe.py
